import React from 'react';

const ContactUs = ({ pathname }) => {
    return (
        <div>
            <h2>ContactUs</h2>
            {pathname}
        </div>
    );
};

export default ContactUs;